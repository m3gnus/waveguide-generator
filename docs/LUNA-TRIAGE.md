# Luna fleet triage ledger — 2026-08-04

Disposition of every finding from the 20-chunk luna-xhigh review fleet (reports: session scratchpad `luna/*.md`, to be archived). Overseer verdicts: ACCEPT unless amended. Cross-cutting notes first, then per-chunk dispositions for the fixer batches. Fixers: implement every ACCEPTED item; cite the finding ID (e.g. S7-1) in code comments only where the constraint is non-obvious.

## Cross-cutting resolutions

1. **QUADRANTS WIRE FORMAT (F1-P0, overseer-verified against v1 `normalise_quadrants`):** canonical values are DIGIT LISTS (1, 12, 14, 1234…), not bitmasks. `serializeDesign` must emit sorted concatenated digits ([1,2,3,4]→1234, [1,2]→12). Add a frontend test pinning all four common domains AND a server-side test asserting the jobs path normalizes 1234→1234, never 15.
2. **SCALE (S5-P1 + S13-P0, one fix):** `DesignConfig.scale` must be applied exactly once, per v1's rule (port from v1 adapter/pipeline — find where v1 applies Scale and mirror it) so preview, solve, and every export see scaled geometry. One shared helper in translate; exports/preview/solver all route through it. Geometry test with scale=2 asserting doubled bounds in preview AND STL/STEP.
3. **Migration 003 scope (S2 amendment):** restrict undefined/NaN dropping to KNOWN numeric parameter keys (schema-derived allowlist); unknown/passthrough keys keep their raw text verbatim.
4. **Legacy aliases (S1-5 + S5):** remove `| str` unions from Literal fields; route legacy values (e.g. inflection 'allow') through migrations, and ensure translate runs AFTER migrations in every path.
5. **Non-finite policy:** finite-validate at the boundaries (schema Expr values, jobs frequency ranges, solver contexts, frame fidelity floats, export coordinates) — reject with 422/typed errors, never let NaN/Inf reach mesher/native/JSON. (S1, S6, S7, S10, S11, S13, S3.)

## Server fixer A — design/protocol/preview (S1–S5)

ACCEPT ALL: S1 (Expr raw-only evaluation; finite guard; OverflowError→ValueError; ConfigBlock ordered duplicate-preserving items; Literal|str removal), S2 (EOF balance errors; FREEFORM grammar validation incl. illegal third values; Freeform.* unknown-key passthrough; migration-003 allowlist per note 3; ratio finiteness; empty cross_sections serialization rule), S3 (unhashable discriminators→FrameError; RecursionError→header-json; fidelity OverflowError; zero-product shape rank/dimension validation mirrored in JS; P2 safe-integer predicate parity), S4 (bounded cooperative cancellation for in-flight to_thread work — abandon-result + per-connection concurrency cap is acceptable; heartbeat/worker task supervision ending the receive loop; strict int fields), S5 (scale per note 2; z_map strip(); non-scalar Expr rejection for structural decisions [depth/quadrant-class]; inflection via migrations; corner_grid explicit 422 if untranslatable; strength/angle combination rejection).

## Server fixer B — jobs/mesh/solver/engines/platform/exports (S6–S13)

ACCEPT ALL: S6 (finite frequency_range; executescript→execute-in-transaction; v1 exported_files validators), S7 (scheduler shutdown guard; atomic complete-vs-cancel transition in store; bounded post-job pruning; finite bounds; gathered-exception logging), S8 (envelope robustness [dict-required, RecursionError, protocol close]; resume cursor advance before live replay; full per-job log persisted to data dir + served over HTTP with tail-only WS; heartbeat validation; strict version check), S9 (gmsh worker shutdown guard + app shutdown hook; strict scalar parsing for solver-control Expr fields [quadrants/depth invalid → validation error, not defaults]; integrity: reject empty meshes + duplicate faces), S10 (circsym-IB aperture tag required; bempp rejects circsym mode; explicit unsupported-kwarg errors for older helpers; native import guards catch OSError; tmpfile path-assignment ordering; finite bounds), S11 (frequency-axis alignment with None padding + failure records; spherical grid validation before publish; beam-shape per-ray invalid-cell rejection; finite-filtered angle argmin; log-domain SPL and DI), S12 (P0: replace pid-file protocol with OS advisory lock (fcntl.flock) held for process lifetime; server/__main__.py forwarding; runtime rotating log handler; dryrun input validation; lock-conflict check BEFORE port scan → exit 2; port TOCTOU → retry-on-bind-failure loop; capabilities probes off-thread + cached; origin port validation; lock OSError → InstanceLockError), S13 (scale per note 2; STEP min-10 restoration; segment bounds → 422; STL finite-coordinate validation; known-extension-only basename stripping).

## Frontend fixer C — F1/F2/F5 (+ F3/F4/F6 appended when their reports land)

ACCEPT ALL so far: F1 (quadrants per note 1; enclosure serialization emits every schema field [edge_type, front/back resolutions, individual space_l/t/r/b] — kill the lossy baffle_margin-only mapping while keeping the UI mirror; drag finalization on family-switch/load), F2 (cursor-gap gating until authoritative resync; per-job generation guards on status refreshes + delete invalidation; rating mutation serialization; compare-selection pruning on deletion; LRU NaN validation; typed fidelity field), F5 (camera refit keyed on bounds identity + nonce; real WebGL2 probe + context-loss fallback; empty-surfaces state; curvature NaN sanitization to neutral).

## Mesher fixer D — M1/M2 (pending reports)

*(to be appended)*

## Frontend fixer C — full disposition (F1–F6)

ACCEPT ALL F1/F2/F5 items (above) plus: F3 (quadrants array↔ATH-digit encoding in BOTH hydrate and serialize — same fix as note 1; enclosure full-field serialization [dup F1]; sampling-mode options fix ['resolution' unsupported, 'zmap' derivable when points present]; legacy Source.Velocity mapping + conditional validation; nullable-import normalization to family defaults [not 0]; NumberField: empty-draft rejection, Escape-cancel suppressing blur-commit, unmount/lost-capture drag cleanup; cross-field f1<f2 validation before commit; profiles-export partial-failure reporting), F4 (color attribute disposal on curvature toggles; real flatShading for *-flat classes + flat zebra variant; edges-mode extraction bounded w/ compact storage; curvature non-finite → neutral [dup F5]), F6 (SPL subtitle from result metadata; clear selection when primary deleted with no replacement; Load-design gated on snapshot presence; rating-race token guard [dup F2 — one fix]; structured error detail formatting; StatusBar values derived from design store, placeholders labeled).

## Mesher fixer D — disposition (M1–M2)

AMENDED M1-4: the finite-difference-of-the-ANALYTIC-SURFACE method REMAINS labeled analytic-parametric (it samples the true surface, never the mesh — original batch-D contract), BUT (a) document that definition in the preview module + metadata notes, and (b) implement M2's coordinate-space corrections so the differences use TRUE parametric coordinates (nonuniform slice_map + FREEFORM φ; open-grid φ passed through) — that is the substantive defect behind the label complaint.
ACCEPT ALL other M1/M2 items: ZMapPoints alias preservation; max_vertices accounting for EVERY surface incl. enclosure + reject caps below topological minimum (no crash path); ellipse/superellipse plan sampling made tolerance-adaptive + fidelity measured against the emitted plan (not a corner formula); chamfered sides split at hard seams with duplicated vertices; zero-angle auto-cap radius=inf → omit/None (JSON-safe metadata, add non-finite metadata guard); achieved silhouette segments reported per surface; fidelity zero-interior-candidate intervals reported unmeasured/cap-limited (never zero-error success); denser reference/midpoint measurement; pathological tolerance/segment preflight clamps.

## 1. What's left / where it works poorly

Verification first: prior C1–C4/C6 fixes are present and passing. Active-span corner validation is in [freeform.py:1042](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/freeform.py:1042), intermediate rounded rectangles activate corner-aware sampling in [profile_sampling.py:704](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/profile_sampling.py:704), state clamps shortened-profile anchors in [state.js:116](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/state.js:116>), CircSym compares complete profile blocks in [axisymmetry.py:101](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/server/solver/axisymmetry.py:101>), and nested contracts forbid unknown fields in [contracts/__init__.py:390](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/server/contracts/__init__.py:390>). I do not re-report them below.

1. **High — FREEFORM has no portable save/load workflow.** Config export hard-rejects it in [src/modules/export/index.js:463](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/export/index.js:463>); the serializer’s non-R-OSSE branch remains OSSE-specific in [src/export/mwgConfig.js:53](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/export/mwgConfig.js:53>); import reports “Could not find OSSE or R-OSSE” in [src/modules/design/useCases.js:17](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/design/useCases.js:17>). Exercised result: FREEFORM config export says the format has no representation, and importing a `FREEFORM` block fails. LocalStorage is therefore the only editable-design persistence. Fixed means typed `Freeform.ProfileH/V/CrossSections` blocks, consuming those blocks during import, preserving arrays and shared fields, and a structural round-trip test including `scale`.

2. **High — switching to FREEFORM is a reset, not an on-ramp.** The model selector calls `GlobalState.update({}, newType)` in [paramPanel.js:486](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/paramPanel.js:486>), replacing the current design with FREEFORM defaults. There is no conversion use case, and [prepareBackendViewportMesh:180](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/geometry/useCases.js:180>) tessellates and discards the built grid. Fixed means the plan §4.2 conversion flow: retain/request the canonical grid, extract H/V cuts, un-scale, decimate to ≤64 anchors, seed stations, truncate rollback explicitly, report deviation, and make the conversion one undoable transaction.

3. **High for manufacturing/CAM — exports are only partially integrated.** STEP and HornLab mesh export are admitted, but STL and profile CSV intentionally reject all server-only formulas at [index.js:358](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/export/index.js:358>) and [index.js:405](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/export/index.js:405>). The errors are clear, but “use a backend-backed export” points to functionality that does not exist. Fixed means generating STL and per-azimuth CSV from the same backend point grid, while retaining STEP as the authoritative CAD path.

4. **Medium-high — corner-radius input and backend validity disagree.** The UI accepts any radius ≥1 mm in [paramPanel.js:924](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/paramPanel.js:924>), while the mesher requires at least 2% of the local half-dimension in [freeform.py:1042](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/freeform.py:1042). On the default 140 mm mouth, entering the exact nominal floor `2.8` reproduces the contradictory error: “must be in [2.8, 140] mm … got 2.8 mm,” caused by an untoleranced floating comparison. Fixed means epsilon-safe bounds plus authoritative, dynamically displayed per-station minimum and active-span maximum; invalid values should remain inline rather than clearing the 3-D view.

5. **Medium-high — authoritative diagnostics exist but never reach the editor.** Python reports deviation, tangent data, and inflection spans in [freeform.py:175](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/freeform.py:175); the adapter attaches them at [mesher_adapter.py:721](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/server/solver/mesher_adapter.py:721>); `prepareBackendViewportMesh` retains metadata at [useCases.js:189](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/geometry/useCases.js:189>), but [scene.js:86](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/app/scene.js:86>) drops it. The editor therefore uses its finite-difference JS mirror. Confirmed example: JS reports `50.000–79.101 mm / 23.51°`; Python reports `50.000–80.138 mm / 24.20°`. Fixed means state-keyed metadata delivery to the editor, visible deviation/status feedback, and Python-authoritative post-build highlighting.

6. **Medium — the 2-D editor is useful but still has dead ends.** Station lines are label-only in [freeformProfileEditor.js:410](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:410>); there is no front-view outline; endpoint tangent handles use a fixed visual arm length and edit only angle in [freeformProfileEditor.js:509](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:509>), despite per-plane strength fields. Backend errors appear only in one raw strip at [paramPanel.js:1153](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/paramPanel.js:1153>), and validation failures deliberately clear the last good viewport in [scene.js:207](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/app/scene.js:207>). Fixed means truthful endpoint strength handles, draggable/mm-labelled stations, an outline inset, targeted error attachment, and retaining a visibly stale last-valid mesh.

7. **Medium — viewport interaction is not actually live in 3-D.** Pointer motion redraws only the SVG preview in [freeformProfileEditor.js:832](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:832>); state and the 3-D rebuild happen once on pointer-up at [freeformProfileEditor.js:893](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:893>). A warmed server viewport build measured about 8.4 ms for a 96×50 grid, so Python evaluation is not the primary problem; transport, panel reconstruction, tessellation, and waiting until release dominate perceived latency. Fixed means an explicitly optimistic drag preview, one authoritative request on release or debounce, and a pending/confirmed visual state.

8. **Medium — rounded corners have measurable acoustic-fit cost, while sharp creases remain unsupported.** Any rounded-rectangle station selects the corner-aware family for every ring in [profile_sampling.py:704](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/profile_sampling.py:704), feeding the acoustic refinement loop at [config_builder.py:2155](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/config_builder.py:2155). In a warmed probe, ellipse-only sampling took 6.8 ms versus about 42 ms for otherwise comparable 5–10 mm rounded corners. Sharp `rectangle` is absent from the accepted shapes at [freeform.py:711](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/freeform.py:711); no crease mask, vertex pinning, sagitta exception, or OCC patch split exists. Fixed means performance budgets for corner schedules first; exact creases later with the full plan §1c treatment, not merely accepting radius zero.

9. **Strategic — optimization remains absent despite being the feature’s stated motivation.** The optimizer’s formula validation still accepts only OSSE/R-OSSE/LOOKUP/ICW in [params.py:402](/Users/magnus/Code/hornlab-workspace/hornlab-optimizer/bem_optimizer/waveguide_geometry/params.py:402), and its transform chain has no FREEFORM branch in [space.py:250](/Users/magnus/Code/hornlab-workspace/hornlab-optimizer/bem_optimizer/genes/space.py:250). Fixed means fixed-dimension H/V radius and tangent genes, a direct mesher builder, geometry reports in candidate metadata, seeded CMA, and gen-0 elitism before serious runs.

10. **Test blind spots.** Geometry coverage is strong, but [test_freeform_server.py:212](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/server/tests/test_freeform_server.py:212>) mocks mesh/STEP builders and [test_freeform_server.py:249](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/server/tests/test_freeform_server.py:249>) tests solve validation rather than an actual BEM solve. Missing coverage includes real FREEFORM STEP generation, one end-to-end BEM frequency, `.mwg` round-trip, conversion with `scale != 1`, rollback truncation, metadata-to-editor handoff, station dragging/inset behavior, the exact 2% corner boundary, and performance budgets. Verification results: mesher 535 passed/23 skipped; targeted server FREEFORM 11/11 passed; frontend 582/584 passed, with only two unrelated sandbox `listen EPERM` failures.

## 2. Assessment of the four proposed simplifications

**(a) One frontend model/codec — DO-NOW, effort M.** Create a module such as `src/config/freeformModel.js` owning `normalizePoint`, `normalizeProfile`, `normalizeStations`, `normalizeFreeformParams`, and `toWireFreeform`. State stores one canonical object-row representation; UI components consume it without local sorting/coercion; the wire encoder alone produces endpoint rows and snake_case fields. This deletes the normalizers in [freeformParams.js:18](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/config/freeformParams.js:18>), [paramPanel.js:157](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/paramPanel.js:157>), [freeformProfileEditor.js:75](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:75>), and [waveguidePayload.js:74](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/solver/waveguidePayload.js:74>), plus the unpublished `profileH/profileV` migration. Risk: pre-W3 browser localStorage designs stop migrating unless a one-release import shim is retained. **Rationale:** do it before round-trip and conversion add two more producers of FREEFORM state.

**(b) Backend-authoritative editor curves/diagnostics — DO-NOW, effort M.** Extend `FreeformGeometry.report()` with display samples, preserve `metadata` through `scene.toRenderMesh`, and publish a state-keyed authoritative-preview event to `FreeformProfileEditor`; while dragging, continue using `buildFreeformDisplayCurve`, mark it pending, then replace samples and spans when the matching viewport result lands. This deletes production use of `computeInflectionSpans` and its finite-difference implementation while retaining the JS Hermite curve as optimistic UI code. Risk: stale responses or lagging highlights unless results are correlated with the existing viewport cache key. **Rationale:** policy warnings should use the same derivatives as the mesher, especially at the 1° threshold.

**(c) One canonical mesher gate — DO-NOW, effort S.** Keep general formula validation in `_validate_formula_features`, but remove its FREEFORM branch; assemble `common`, then call one public `validate_freeform_config(common)` implementation. `build_point_grid` must still invoke that same function because it is a supported bypass entry point—“one gate” should mean one implementation, not only one runtime call site. This deletes the duplicated morph/gcurve/cross-section/extension checks at [config_builder.py:453](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/config_builder.py:453) while retaining the calls at [config_builder.py:1103](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/config_builder.py:1103) and [profile_sampling.py:698](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/hornlab_mesher/profile_sampling.py:698). Risk: hand-written configs may see later or differently wrapped errors. **Rationale:** the normalized `common` block already contains every feature value required by the shared validator.

**(d) Drop `cornerRatio` and inflection `allow` — DO-NOW, effort S.** Remove `corner_ratio` from the server contract/adapter, remove ratio parsing and descriptors from the mesher, remove the legacy UI hint and payload branch, and reduce inflection policy to `warn|reject`. This deletes a second, semantically different corner model and a policy/cache dimension whose output is explicitly identical to `warn` in [test_freeform_core.py:619](/Users/magnus/Code/hornlab-workspace/hornlab-waveguide-mesher/tests/test_freeform_core.py:619). Risk: only unpublished branch configs and localStorage; `cornerRatio` users lose proportional-radius behavior, so confirm absolute millimetres remain the owner’s intended contract. **Rationale:** compatibility aliases become expensive once `.mwg` is published.

## 3. Assessment of the three endorsed UX items

**(a) Scrubbable cross-section inset — effort M.** Mount a small SVG inset and depth slider beside the existing toolbar in [freeformProfileEditor.js:168](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:168>). Extend viewport metadata with a compact authoritative outline atlas generated through `FreeformGeometry.cross_section_radius()`—for example, merged station/anchor depths plus a bounded regular t-grid and fixed φ samples. The inset interpolates that atlas for committed state and uses local Hermite/outline math only while an edit is pending. Show `t`, depth in mm, active station span, shape blend, H/V axes, and corner radius. Owner decisions: full outline versus one quadrant, atlas density/payload budget, whether to show the wall offset, and whether scrubbing selects a station or remains inspection-only.

**(b) Convert current design to FREEFORM — effort L.** Add a design conversion use case alongside [src/modules/design/useCases.js](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/design/useCases.js>) and a raw-grid-returning sibling to [prepareBackendViewportMesh](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/modules/geometry/useCases.js:141>). Intercept the model selector at [paramPanel.js:506](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/paramPanel.js:506>) with “Start blank” versus “Convert current design.” Request the canonical full-domain grid; extract φ=0 and φ=90° inner cuts; divide coordinates by active `scale`; truncate non-monotone rollback at max-z with a message; RDP-decimate by normal error while preserving endpoints/features and the 64-anchor limit; fit endpoint tangents; seed morph-derived shape stations; build the candidate FREEFORM grid; and show max H/V/full-surface deviation before committing one undoable state replacement. Owner decisions: decimation tolerance/default anchor budget, whether approximate gcurve/h-bulge conversions are allowed, rollback confirmation wording, morph-to-smootherstep acceptance tolerance, and whether the pre-conversion design remains as an editor ghost.

**(c) Draggable station lines with mm depth — effort S.** Give interior lines created by [drawStations](</Users/magnus/Code/hornlab-workspace/Waveguide Generator/src/ui/freeformProfileEditor.js:410>) `data-handle="station"` and extend the existing pointer state machine. During motion, convert x to `t=z/length`, clamp between neighboring stations, update the local `crossSections` clone, redraw the line/inset, and label it `48.0 mm · t=0.400`; commit the complete station list once on pointer-up so the existing table refreshes through state. Keep throat/mouth locked. Owner decisions: snapping increment, minimum neighbor spacing, whether lines may cross and reorder or only clamp, and modifier-key behavior for fine motion.

## 4. Recommended execution order

1. Fix epsilon-safe corner bounds and expose authoritative per-station limits in the UI — **S**.
2. Consolidate the frontend FREEFORM model/codec and remove unpublished aliases — **M**.
3. Collapse mesher feature checks into the shared canonical validator — **S**.
4. Preserve viewport metadata and make curve samples/diagnostics backend-authoritative — **M**.
5. Implement `.mwg` FREEFORM serialization/import and allow config export — **M**.
6. Add draggable station lines with millimetre readouts — **S**.
7. Add the authoritative cross-section outline atlas and scrubber inset — **M**.
8. Implement built-grid conversion with un-scaling, decimation, deviation review, and rollback messaging — **L**.
9. Add backend STL/profile-CSV paths plus real FREEFORM STEP and one-frequency BEM acceptance tests — **M/L**.
10. Profile and reduce global corner-aware acoustic sampling cost; add regression budgets — **M**.
11. Add the seeded fixed-dimension FREEFORM optimizer builder and geometry guards — **L**.
12. Implement exact sharp-corner crease topology only after the smooth-corner BEM path is stable — **L**.
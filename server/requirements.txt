# MWG Horn BEM Solver Backend Requirements
# Supported runtime matrix: Python >=3.10,<3.15

# Web Framework
fastapi>=0.104.1,<1.0
uvicorn[standard]>=0.24.0,<1.0
python-multipart>=0.0.6,<1.0

# BEM Solver (bempp-cl, supported range >=0.4,<0.5)
# Note: Install prerequisites first (see README.md)
numpy
scipy
numba
meshio>=4.0.16

# The bempp-cl package should be installed via:
# pip install 'bempp-cl>=0.4,<0.5'
# If your platform lacks wheels, use the upstream git repository pinned to a 0.4.x release.

# Mesh Processing
trimesh>=4.0.5,<5.0
# Gmsh Python API is managed separately in server/requirements-gmsh.txt
# because wheel availability can vary by platform.

# Plotting
matplotlib

# Utilities
pydantic>=2.10.0,<3.0

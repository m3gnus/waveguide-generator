# MWG Horn BEM Solver Backend Requirements
# Supported runtime matrix: Python >=3.10

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Scientific computing (also prerequisites for bempp-cl if installed separately)
numpy
scipy
meshio>=4.0.16

# Mesh Processing
trimesh>=4.0.5
# Gmsh Python API is managed separately in server/requirements-gmsh.txt
# because wheel availability can vary by platform.

# Plotting
matplotlib

# Utilities
pydantic>=2.10.0,<3.0

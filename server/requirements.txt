# MWG Horn BEM Solver Backend Requirements
# Supported runtime matrix: Python >=3.10

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Scientific computing
numpy
scipy
meshio>=4.0.16

# Pinned VCS dependencies for reproducible public installs.
git+https://github.com/m3gnus/hornlab-waveguide-mesher.git@8a8f3837da20416d3865b1a5bb1f65906a232764
git+https://github.com/m3gnus/hornlab-metal-bem.git@4bcf889cf8f1740b0a4b8de734624cd434f7dcbe

# Canonical plot renderer (theme-aware result charts). If this install is
# absent the backend falls back to the in-repo server/solver charts renderer,
# so it is not hard-required. Development overrides should be installed
# explicitly into the active environment rather than discovered by path.
git+https://github.com/m3gnus/hornlab-plots.git@52cb9c41f1efc67e038c16347088bd0eefee76af

# Optional non-Metal fallback backend lives in server/requirements-bempp.txt.

# hornlab-waveguide-mesher owns the production Gmsh mesh build dependency.

# Plotting
matplotlib

# Utilities
pydantic>=2.10.0,<3.0

# MWG Horn BEM Solver Backend Requirements
# Supported runtime matrix: Python >=3.10

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Scientific computing
numpy
scipy
meshio>=4.0.16

# Pinned VCS dependencies for reproducible public installs.
git+https://github.com/m3gnus/hornlab-waveguide-mesher.git@ce576fa78bdd2a3f5b5f073e4e55c7228bbe16f8
git+https://github.com/m3gnus/hornlab-metal-bem.git@09f3b0b0e99b23936cac31531b1f82c6e369ea44

# Canonical plot renderer (theme-aware result charts). If this install is
# absent the backend falls back to the in-repo server/solver charts renderer,
# so it is not hard-required. Local dev may `pip install -e ../hornlab-plots`
# to pick up sibling edits.
git+https://github.com/m3gnus/hornlab-plots.git@916ed784bb026838f47a380c542638da32080fa3

# Optional non-Metal fallback backend lives in server/requirements-bempp.txt.

# hornlab-waveguide-mesher owns the production Gmsh mesh build dependency.

# Plotting
matplotlib

# Utilities
pydantic>=2.10.0,<3.0

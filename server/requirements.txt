# MWG Horn BEM Solver Backend Requirements
# Supported runtime matrix: Python >=3.10

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Scientific computing
numpy
scipy
meshio>=4.0.16

# Pinned VCS dependencies for reproducible public installs.
git+https://github.com/m3gnus/hornlab-waveguide-mesher.git@6eb4a140e47a56062d28ecac773cf7e731526e33
git+https://github.com/m3gnus/hornlab-metal-bem.git@70c18fd9bde22e1e1e20e195f994a8fdc2a11f67

# Canonical plot renderer (theme-aware result charts). Installed locally as an
# editable sibling checkout (../hornlab-plots, resolved from the repo root where
# `pip install -r server/requirements.txt` is normally run). Once hornlab-plots
# has a public remote, replace this line with a git+https@<SHA> pin like the two
# lines above. If the sibling checkout is absent, the backend falls back to the
# in-repo server/solver charts renderer, so this install is not hard-required.
-e ../hornlab-plots

# Optional non-Metal fallback backend lives in server/requirements-bempp.txt.

# hornlab-waveguide-mesher owns the production Gmsh mesh build dependency.

# Plotting
matplotlib

# Utilities
pydantic>=2.10.0,<3.0
